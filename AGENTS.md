# DohaMusic Codex 작업 지침

이 문서는 DohaMusic 저장소에서 작업하는 Codex 및 기타 개발 에이전트가 반드시 따라야 하는 최상위 작업 규칙이다.

모든 작업자는 작업을 시작하기 전에 이 문서를 먼저 확인해야 한다.

이 문서의 적용 범위는 저장소 루트와 모든 하위 디렉터리이다.

> 문서 상태: [완료]
> 최종 수정일: 2026-07-29
> 관련 기능: 저장소 전체 작업 절차
> 관련 문서: [README](README.md), [개발 로드맵](ROADMAP.md), [변경 이력](CHANGELOG.md)

## 1. 프로젝트 기본 정보

| 항목 | 값 |
|---|---|
| 프로젝트명 | DohaMusic |
| 저장소 | `DDORINY/DohaMusic` |
| 개발 기준 브랜치 | `develop` |
| 안정화 브랜치 | `main` |
| 기본 문서 언어 | 한국어 |
| 커밋 언어 | 한국어 |
| PR 제목 및 본문 언어 | 한국어 |

DohaMusic은 공개된 사전학습 음악 생성·가창·음색 변환 모델을 활용하여, 사용자가 작성한 프롬프트나 가사를 기반으로 음악을 생성하고 사용자 본인의 목소리로 노래하는 결과물을 만드는 개인 음악 생성 AI 프로젝트이다.

## 2. 지침 우선순위와 작업 원칙

- 작업 시작 전에 이 문서, 현재 브랜치, 작업 트리, 원격 상태와 관련 문서를 확인한다.
- 사용자 요청, 저장소의 더 하위 경로에 있는 `AGENTS.md`, 이 문서 순으로 적용하되 충돌이 있으면 더 구체적인 지침을 따른다.
- 요청 범위만 변경하고 관련 없는 정리·리팩터링을 섞지 않는다.
- 실제로 확인한 사실과 실행한 검증만 보고한다.
- 구현되지 않은 기능, 실행하지 않은 테스트·실험, 미확인 모델 성능·라이선스를 완료 또는 검증됨으로 표시하지 않는다.
- 코드·설정 변경으로 기능, 구조, API, 데이터, 실행 방법, 상태 또는 정책이 달라지면 관련 문서와 변경 이력도 같은 작업에서 최신화한다.

## 3. 모델 개발 원칙

- 공개 사전학습 모델을 활용한다.
- 음악 생성, 음원 분리, 음색 변환, 믹싱 모듈을 독립적으로 구성한다.
- 특정 모델에 전체 서비스가 종속되지 않도록 어댑터 구조를 사용한다.
- 모델 도입 전에 코드, 가중치, 데이터셋 라이선스와 상업적 이용 가능 여부를 확인한다.
- 확인되지 않은 모델 성능이나 지원 기능을 사실처럼 문서화하지 않는다.
- 개인 GPU 환경인 RTX 3060 Ti 8GB를 우선 고려하고 모델을 단계별로 로드·해제한다.
- 대규모 기반 모델 직접 사전학습은 현재 범위에서 제외한다.

기본 파이프라인은 다음과 같다.

```text
사용자 프롬프트 또는 가사
→ 음악 및 임시 가창 생성
→ 보컬과 반주 분리
→ 사용자 본인 음색으로 보컬 변환
→ 보컬과 반주 믹싱
→ 완성곡 생성
```

## 4. Git 브랜치 전략

### 브랜치 역할

- `main`: 완성되거나 안정화된 버전만 반영한다.
- `develop`: 일반 개발과 문서 작업이 최종 병합되는 통합 브랜치다.
- 작업 브랜치: 요청 한 건의 실제 변경을 수행한다.

일반 작업에서 다음 행위를 금지한다.

- `main` 직접 커밋 또는 직접 푸시
- 작업 브랜치 없이 `develop`에서 직접 수정
- 검증하지 않은 변경 병합
- PR 없이 `develop`에 직접 병합
- 사용자의 명시적 릴리스 요청 없이 `develop → main` 병합

`develop → main`은 MVP 완료, 문서·구현 일치, 필수 테스트 통과, 주요 오류 해결, 배포 가능 상태를 확인하고 사용자가 명시적으로 요청한 안정화·릴리스 작업에서만 수행한다.

### develop 브랜치가 없는 경우

1. 원격을 최신화하고 `main` 상태와 깨끗한 작업 트리를 확인한다.
2. 현재 `main` 커밋에서 `develop` 브랜치를 생성해 원격에 푸시한다.
3. 문서나 코드 변경은 추가하지 않은 채 브랜치 생성 자체를 분리한다.
4. `develop`에서 별도의 작업 브랜치를 생성한다.

## 5. 작업 브랜치 규칙

작업 브랜치는 최신 `develop`에서 만들고 영문 소문자와 하이픈을 사용한다. 하나의 브랜치에는 하나의 명확한 목적만 둔다.

```text
docs/<작업명>
feat/<작업명>
fix/<작업명>
refactor/<작업명>
test/<작업명>
chore/<작업명>
```

예: `docs/add-project-guidelines`, `feat/music-generation-api`, `fix/audio-upload-validation`.

## 6. 작업 시작 체크리스트

- [ ] 이 `AGENTS.md`를 읽었다.
- [ ] 현재 및 원격 브랜치와 원격 저장소를 확인했다.
- [ ] `develop`을 최신화했다.
- [ ] 작업 트리가 깨끗하다.
- [ ] 최신 `develop`에서 작업 브랜치를 만들었다.
- [ ] 사용자 요청 범위와 관련 문서를 확인했다.
- [ ] 기존 구현·문서·사용자 변경과 충돌 가능성을 확인했다.
- [ ] 문서 영향도를 미리 판정했다.

## 7. 작업 한 건의 필수 흐름

```text
1. AGENTS.md 확인
2. 저장소 상태 및 요청 범위 확인
3. develop 최신화
4. develop 기준 작업 브랜치 생성
5. 작업 수행
6. 관련 테스트 및 검증
7. 문서 영향도 확인 및 관련 문서 최신화
8. CHANGELOG 변경 이력 기록
9. 변경 파일과 비밀·대용량 파일 포함 여부 검토
10. 한국어 커밋 생성
11. 원격 작업 브랜치 푸시
12. develop 대상 PR 생성
13. PR 변경 내용·문서 영향·검증·충돌·병합 가능 여부 확인
14. PR을 develop에 병합
15. develop 병합 결과와 원격 상태 확인
16. 최종 결과 보고
```

문서 최신화와 `CHANGELOG` 기록이 필요한데 누락된 작업은 완료로 판단하지 않는다. 문서 변경이 필요하지 않으면 PR 본문과 최종 보고에 구체적인 이유를 남긴다.

## 8. 변경 범위와 금지 사항

- 관련 없는 코드 정리나 요청하지 않은 대규모 리팩터링을 하지 않는다.
- 기존 파일, 사용자 데이터, 유효한 문서·변경 이력·ADR을 임의로 삭제하지 않는다.
- 환경 변수의 실제 값, 비밀키, 토큰을 변경하거나 커밋하지 않는다.
- WAV, MP3, 개인 음성 원본, 모델 가중치·체크포인트 등 대용량·민감 파일을 무단 커밋하지 않는다.
- 발견한 별도 문제는 요청 범위가 아니면 수정하지 않고 후속 작업으로 보고한다.
- 기존 커밋을 임의로 amend, rebase 또는 강제 푸시해 이력을 재작성하지 않는다.

## 9. 커밋 규칙

모든 커밋 메시지는 다음 형식의 한국어로 작성한다.

```text
<type>: <구체적인 한국어 작업 요약>
```

허용 type 예시는 `docs`, `feat`, `fix`, `refactor`, `test`, `chore`, `build`, `ci`다.

좋은 예:

```text
docs: DohaMusic 저장소 작업 규칙 문서화
feat: 가사 기반 음악 생성 요청 기능 추가
fix: 음성 파일 업로드 검증 오류 수정
```

`update`, `fix`, `수정`, `작업 완료`, `임시 커밋`, `test commit`처럼 작업 내용을 알 수 없는 메시지는 금지한다.

## 10. Pull Request 규칙

- 일반 PR의 대상은 `develop`이며 흐름은 `작업 브랜치 → develop`이다.
- 일반 작업에서 `작업 브랜치 → main` 또는 임의의 `develop → main` PR을 만들지 않는다.
- PR 제목과 본문은 한국어로 작성하고 제목은 커밋 요약과 일치시킨다.
- 기존 저장소 병합 관례를 우선하고 관례가 없으면 한 작업을 하나의 변경 단위로 유지하는 squash merge를 우선 검토한다.

PR 본문에는 다음 구조와 문서 체크리스트를 포함한다.

```markdown
## 작업 내용

## 주요 변경 사항

## 변경 파일

## 검증 결과

## 영향 범위

## 미구현 또는 후속 작업

## 문서 영향

- [ ] README를 검토했다.
- [ ] ROADMAP을 검토했다.
- [ ] CHANGELOG에 변경 이력을 기록했다.
- [ ] 관련 기능 및 설계 문서를 최신화했다.
- [ ] API 또는 데이터베이스 변경 문서를 반영했다.
- [ ] ADR 작성 또는 수정 필요 여부를 검토했다.
- [ ] 실험 보고서 작성 필요 여부를 검토했다.
- [ ] 문서와 실제 구현이 일치하는지 확인했다.

### 수정한 문서

- `문서 경로`: 변경 내용

### 문서 변경이 없는 경우

문서 변경이 필요하지 않은 이유를 구체적으로 작성한다.

## 체크리스트

- [ ] 요청 범위를 충족했다.
- [ ] 관련 검증을 수행했다.
- [ ] 기존 기능에 불필요한 변경이 없다.
- [ ] 문서와 구현 내용이 일치한다.
- [ ] 보안 및 음성 데이터 정책을 위반하지 않는다.
```

PR 생성 후 base가 `develop`인지, 변경 파일과 범위, 불필요한 파일, 검증 결과, 문서 영향, 충돌과 병합 가능 여부를 확인한다.

## 11. PR 병합과 완료 기준

병합 전에 테스트·검증 통과, 대상 브랜치, 변경 범위, `main` 무변경, 바이너리·모델·비밀정보 부재를 확인한다. 병합 후 PR이 `merged`인지, `develop`에 정확한 변경이 반영됐는지, 원격 `develop`이 정상인지 확인한다.

다음을 모두 만족해야 작업 완료로 판단한다.

- 요청한 변경과 관련 테스트·검증 완료
- 영향받는 문서 확인과 필요한 최신화 완료
- `CHANGELOG` 기록 완료
- 필요한 ADR 또는 실험 보고서 작성 완료
- 변경 사항 커밋 및 원격 작업 브랜치 푸시 완료
- `develop` 대상 PR과 문서 영향 검토 완료
- PR의 `develop` 병합과 병합 결과 확인 완료
- 코드와 문서가 최신 상태이고 `main`은 변경되지 않음

## 12. 검증 규칙

작업 종류에 맞는 검증을 커밋 전에 수행하고 실행할 수 없는 항목은 생략 이유를 보고한다.

### 문서

- Markdown 링크와 상대 경로
- 빈 문서와 동일 내용의 중복 문서
- Mermaid fence와 기본 문법
- README 문서 링크
- 용어, 상태, 실제 구현과의 일관성

### Backend

- 관련 단위 테스트, API 실행, 타입 검사, Lint, 오류 응답

### Frontend

- Lint, 타입 검사, 빌드, 주요 화면 렌더링, API 연결 영향

### AI 파이프라인

- 모델 인터페이스, 입력 검증, 출력 생성
- GPU와 CPU fallback, 오류 처리, 작업 상태 전이
- 모델·버전·환경·VRAM·품질 결과 기록

## 13. 모든 작업의 문서 영향도 확인

작업 시작과 종료 시 다음 위치의 영향 여부를 검토한다. 모든 문서를 매번 수정하지 않고 직접 영향을 받는 문서만 수정한다.

```text
README.md
ROADMAP.md
CHANGELOG.md
AGENTS.md
docs/00-overview/
docs/01-research/
docs/02-requirements/
docs/03-architecture/
docs/04-models/
docs/05-data/
docs/06-api/
docs/07-database/
docs/08-evaluation/
docs/09-security/
docs/10-operations/
docs/11-decisions/
planning/
reports/
```

문서 변경이 없으면 다음처럼 구체적으로 설명한다.

```text
문서 변경 없음
사유: 기존 문서에 정의된 내부 구현의 오타 수정이며 외부 동작, API, 구조, 설정 및 사용 방법에 변화가 없음.
```

## 14. 작업 유형별 우선 검토 문서

| 작업 유형 | 우선 검토 대상 |
|---|---|
| 기능 추가 | README, ROADMAP, CHANGELOG, 요구사항, 사용자 시나리오, 아키텍처, API, DB, 운영 문서 |
| 버그 수정 | CHANGELOG, troubleshooting, 관련 API·기능·검증 문서 |
| 리팩터링 | CHANGELOG, 시스템·Backend·Frontend 아키텍처, 모델 어댑터, ADR |
| API 변경 | CHANGELOG, api-overview, 상세 API, error-codes, README 예시, Frontend 연동 |
| DB 변경 | CHANGELOG, ERD, table-definition, job-state-model, 마이그레이션 문서 |
| AI 모델·파이프라인 | CHANGELOG, model-comparison, model-selection-policy, ai-pipeline, 로딩·GPU·평가·실험 문서 |
| 환경·배포 | CHANGELOG, local-development, environment-variables, deployment-guide, README |
| 보안·음성 정책 | CHANGELOG, security-policy, voice-consent-policy, abuse·upload·content 정책, 동의 DB 구조 |

API 변경은 경로, Method, 요청·응답 필드, 상태·오류 코드, 인증과 호환성을 기록한다. DB 변경은 테이블, 컬럼, 인덱스, 관계, 상태값과 Mermaid ERD를 갱신한다. 모델 변경은 이름·버전·기능·도입/제거 이유·라이선스·VRAM·환경·추론·품질·기존 모델 차이를 확인된 범위에서 기록한다.

## 15. CHANGELOG 관리

- 일반 작업은 `CHANGELOG.md`의 `[Unreleased]` 아래 `추가`, `변경`, `수정`, `제거`, `보안`, `문서` 범주에 기록한다.
- 사용자가 이해할 수 있는 구체적 변경을 적고 커밋 해시만 나열하지 않는다.
- 버전이 확정되거나 `develop → main` 릴리스 시 해당 내용을 `## [버전] - YYYY-MM-DD` 섹션으로 이동한다.
- 기존 변경 이력을 삭제하거나 모호한 `코드 수정`, `기능 업데이트`로 대체하지 않는다.
- 같은 내용을 여러 문서에 복사하지 않고 전문 문서에 대표 설명을 두고 링크한다.

## 16. 문서 작성과 메타데이터

- 프로젝트 문서는 한국어로 작성하되 기술명, 모델명, 코드 식별자, API, 환경 변수, 파일·브랜치명은 영문을 유지할 수 있다.
- 기능 상태는 `[완료]`, `[진행 중]`, `[계획]`, `[검토 필요]`, 필요 시 `[보류]`, `[제외]`로 명시한다.
- 미확인 사항은 `공식 문서 확인 필요`, `라이선스 검토 필요`, `로컬 테스트 필요`, `검증 필요`로 표시한다.
- 주요 문서에는 가능한 경우 문서 상태, 최종 수정일, 관련 기능, 관련 문서 링크를 둔다.
- 최종 수정일은 문서를 실질적으로 변경한 날의 한국 표준시 `YYYY-MM-DD`이며 날짜만 갱신하지 않는다.
- 기능 상태가 바뀌면 README와 ROADMAP의 상태를 함께 검토한다. 구현, 테스트, 문서, CHANGELOG, `develop` 병합 전에는 ROADMAP을 `[완료]`로 표시하지 않는다.
- 범위에서 제거된 항목은 지우지 않고 `[보류]` 또는 `[제외]`와 이유를 남긴다.

## 17. ADR 이력 관리

핵심 모델, DB, 작업 큐, Storage, 인증·권한, AI 파이프라인 순서, 음성 보관 정책, 호환성을 깨는 API, 주요 기술 스택 변경은 ADR을 작성하거나 갱신한다.

파일명은 `ADR-XXX-결정-내용.md` 형식으로 하고 상태, 작성일, 최종 수정일, 배경, 문제, 결정, 선택 이유, 대안, 장단점, 영향, 마이그레이션, 재검토 조건, 관련 PR을 포함한다. 결정을 바꾸면 기존 ADR을 삭제하지 않고 `대체됨` 또는 `폐기됨`으로 표시해 새 ADR과 연결한다.

## 18. AI 실험 이력

모델 실행, 성능 비교, GPU 측정, 음질 평가를 수행하면 `reports/experiments/EXP-XXX-설명.md`에 다음을 기록한다.

- 실험 ID·날짜·목적, 작업 브랜치, 관련 커밋·PR
- 모델·버전, 실행 환경, GPU·VRAM
- 안전하게 기록 가능한 프롬프트·가사·샘플 조건
- 실행 명령·설정, 실행 시간·최대 VRAM
- 결과 파일의 안전한 상대 경로 또는 식별자
- 평가, 문제, 결론, 후속 실험

개인 음성 원본과 생성 음원 자체는 Git에 커밋하지 않는다.

## 19. 문서와 구현 일치 검증

작업 종료 전에 README와 실제 기능, API 문서와 endpoint, 환경 변수 문서와 설정, ERD와 데이터 모델, 작업 상태 문서와 상태값, 모델 문서와 실제 모델, ROADMAP과 구현 상태, 실험 보고서와 실제 결과를 비교한다.

충돌 시 실제 구현을 사실 기준으로 문서를 갱신한다. 구현이 요구사항과 다르면 문서만 몰래 맞추지 말고 문제를 보고하고 요청 범위에서 구현 수정이 가능한지 판단한다.

## 20. 음성 데이터와 안전

- 본인 음성 또는 명시적 동의를 받은 음성만 사용한다.
- 타인의 음성 무단 복제 기능을 구현하지 않는다.
- 음성 동의 기록과 생성 결과의 사용 음성 프로필·AI 생성 여부를 보존한다.
- 원본 음성 삭제와 파생 데이터 처리 정책을 설계·구현에 반영한다.
- 업로드 파일 접근 권한을 관리한다.
- 비밀정보, 개인 음성 원본, 생성 음원, 모델 체크포인트는 Git에 커밋하지 않는다.
- 필요 시 `.gitignore`를 보완하되 기존 유효 규칙을 삭제하지 않는다.

## 21. 개발 및 구현 원칙 (Development Principles)

DohaMusic은 단기 기능 구현 프로젝트가 아니라 장기적으로 유지·확장 가능한 AI 플랫폼을 목표로 한다. 모든 구현과 설계는 다음 우선순위를 따른다.

```text
정확성
→ 안정성
→ 유지보수성
→ 확장성
→ 성능
→ 개발 속도
```

개발 속도를 위해 프로젝트 구조나 품질 기준을 훼손하지 않는다.

### 구현 전 필수 분석

기능 구현을 바로 시작하지 않고 다음 절차를 먼저 수행한다.

```text
1. 기존 코드 분석
2. 기존 문서 확인
3. 영향 범위 분석
4. 기존 구현 방식 조사
5. 구현 계획 수립
6. 이후 구현 시작
```

기존 기능과 중복되는 구현이 없는지 반드시 확인한다.

### 기존 구현 우선

새 기능을 만들기 전에 다음 순서로 기존 코드를 재사용하거나 확장할 수 있는지 확인한다.

```text
기존 기능 재사용
→ 기존 기능 확장
→ 리팩터링
→ 새 구현
```

동일한 책임을 가진 클래스, 함수 또는 API를 새로 만드는 것을 지양한다.

### 프로젝트 구조 유지

현재 프로젝트의 아키텍처와 계층 경계를 우선하며 새로운 구조를 임의로 도입하지 않는다. 예를 들어 다음 구조가 채택되어 있다면 상위 계층이 하위 계층을 우회하거나 직접 접근하게 만들지 않는다.

```text
Frontend
→ API
→ Service
→ Repository
→ Model
```

### 인터페이스 중심 개발

AI 모델은 교체 가능하도록 인터페이스 또는 Adapter 구조로 격리한다. 특정 모델 라이브러리에 직접 의존하는 코드를 서비스 전체에 퍼뜨리지 않는다.

```text
MusicGenerator
├─ AceStepAdapter
├─ StableAudioAdapter
└─ FutureAdapter
```

모델을 교체할 때 서비스 코드 수정이 최소화되어야 한다.

### 추측 구현 금지

공식 문서에서 확인되지 않은 기능은 구현하지 않는다. 다음 행위를 금지한다.

- 모델이 지원할 것이라고 추측하는 행위
- 공식 API 형태를 추측하는 행위
- 지원 여부를 확인하지 않은 옵션 구현
- 검증하지 않은 라이선스 사용
- 공식 문서나 재현 가능한 근거가 없는 기능 구현

불확실한 항목은 `검증 필요` 또는 `공식 문서 확인 필요`로 기록한다.

### 하드코딩 금지

다음 정보는 코드에 직접 작성하지 않고 설정 파일 또는 환경 변수로 관리한다.

- 모델 이름과 모델 경로
- GPU 번호
- API URL과 포트 번호
- 파일 경로
- 환경별 설정
- 비밀정보

비밀정보는 안전한 비밀 저장 수단을 사용하고 설정 예제에는 실제 값을 넣지 않는다.

### TODO 관리

단순한 `TODO`만 남기지 않는다. TODO에는 필요한 이유, 구체적인 후속 작업, 관련 문서 또는 이슈, 우선순위를 함께 기록한다.

### 로그 정책

AI 관련 작업은 최소한 다음 이벤트와 측정값을 구조화해 기록한다.

- 작업 시작과 완료
- 모델 로드
- 추론 시작과 종료
- 예외 발생
- GPU 사용량
- 처리 시간

예외를 숨기거나 성공으로 변환하지 않는다. 민감 데이터와 비밀정보는 로그에 남기지 않는다.

### 긴 작업 처리

5초 이상 걸릴 가능성이 있는 작업은 비동기 작업으로 설계하고 직접 HTTP 요청에서 완료를 기다리지 않는다.

```text
요청
→ Job 생성
→ Queue
→ Worker
→ 완료
```

### 테스트 우선

새 기능은 테스트 없이 완료 처리하지 않는다. 가능한 경우 성공, 실패, 예외, 회귀 테스트를 모두 검토한다. 실행하지 못한 테스트가 있으면 항목과 이유를 최종 보고와 PR 본문에 기록한다.

### 성능 기록

AI 관련 변경은 가능한 경우 다음 항목을 실험 보고서에 기록한다.

- 실행 시간
- GPU와 VRAM
- 모델 버전
- 입력 크기
- 출력 길이
- 품질 평가

측정하지 않은 값은 추측하지 않고 `검증 필요`로 남긴다.

### 문서 우선

구현이 변경되면 문서도 함께 변경해 문서와 구현을 일치시킨다. README, ROADMAP, CHANGELOG, 관련 설계·API 문서, ADR, 실험 보고서를 우선 검토한다.

### Architecture Decision Record

새 AI 모델 도입, 핵심 구조, DB 구조, API 호환성, Queue, Storage 또는 보안 정책 변경은 ADR 작성 여부를 반드시 검토한다. ADR 없이 프로젝트 방향을 크게 변경하지 않는다.

### 코드 품질

- 한 함수는 하나의 책임만 가진다.
- 한 클래스는 하나의 역할만 가진다.
- 중복 코드는 공통화한다.
- 500줄 이상의 파일은 책임 분리를 검토한다.
- 100줄 이상의 함수는 리팩터링을 검토한다.

줄 수 기준만으로 기계적으로 분리하지 않고 응집도와 책임 경계를 함께 판단한다.

### 프로젝트 일관성

새 기술이나 구조를 적용하기 전에 기존 아키텍처, 네이밍, 폴더 구조, 코딩 스타일, 문서 구조, Git 전략과 구현 방식을 따른다. 더 나은 방법이 있더라도 국소적인 예외보다 프로젝트 전체의 일관성을 우선한다. 새로운 패턴이 반드시 필요하면 이유와 영향·대안을 설명하고 ADR에 기록한 뒤 적용한다.

### 구현 완료 기준

다음 조건을 모두 만족해야 구현 완료로 판단한다.

- 기능 구현 완료
- 관련 테스트 완료
- 관련 문서 최신화
- CHANGELOG 기록
- 관련 ADR 작성 또는 갱신 필요 여부 검토
- 한국어 커밋
- 원격 작업 브랜치 푸시
- `develop` 대상 PR 생성
- `develop` 병합
- 병합 후 최종 검증 완료

하나라도 누락되면 구현 완료로 보고하지 않는다.

## 22. 작업 종료 체크리스트

- [ ] 요청 작업과 관련 검증을 완료했다.
- [ ] 불필요한 파일을 수정하지 않았고 비밀정보·민감·대용량 파일을 포함하지 않았다.
- [ ] 변경된 기능과 구조에 영향을 받는 문서를 확인했다.
- [ ] 관련 문서를 현재 구현에 맞게 수정했다.
- [ ] README 최신화 필요 여부와 ROADMAP 상태를 확인했다.
- [ ] CHANGELOG에 구체적인 변경 이력을 기록했다.
- [ ] ADR 작성 또는 갱신 필요 여부를 확인했다.
- [ ] AI 실험을 수행했다면 실험 보고서를 작성했다.
- [ ] 문서와 실제 구현이 일치하는지 확인했다.
- [ ] 한국어 커밋을 만들고 작업 브랜치를 원격에 푸시했다.
- [ ] `develop` 대상 PR을 만들고 문서 수정 내역과 검증 결과를 기록했다.
- [ ] PR 내용·충돌·병합 가능 여부를 확인하고 `develop`에 병합했다.
- [ ] 원격 `develop` 반영을 확인했다.
- [ ] `main`을 변경하지 않았다.
- [ ] 최종 보고를 작성했다.

## 23. 최종 보고 형식

1. 작업 요약
2. 생성·수정한 코드 파일
3. 생성·수정한 문서 파일
4. 문서 최신화 내용
5. CHANGELOG 기록 내용
6. ROADMAP 상태 변경
7. ADR 작성 또는 변경 여부
8. 실험 보고서 작성 여부
9. 검증한 항목
10. 검증 결과
11. 작업 브랜치
12. 커밋 메시지
13. 커밋 해시
14. 원격 푸시 결과
15. PR 번호 및 제목
16. PR 대상 브랜치
17. `develop` 병합 결과
18. `main` 변경 여부
19. 미확정 사항 또는 발견된 문제
20. 후속 작업

단순히 “작업 완료”라고만 보고하지 않는다.
