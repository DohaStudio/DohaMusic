import { describe,expect,it,vi } from "vitest";import { pollingInterval } from "@/hooks/use-pipeline";import type { PipelineJobDto } from "@/types/api";
const job=(status:PipelineJobDto["status"]):PipelineJobDto=>({id:"j",voice_profile_id:"v",status,current_step:"step",progress_percent:0,prompt:"p",lyrics:null,genre:null,duration_seconds:30,seed:null,pipeline_version:"1",result_metadata:{},failed_step:null,error_code:null,error_message:null,created_at:"",updated_at:"",completed_at:null});
describe("adaptive polling",()=>{it("초기에는 1초, 이후에는 2초다",()=>{expect(pollingInterval({state:{data:job("GENERATING"),dataUpdateCount:1}})).toBe(1000);expect(pollingInterval({state:{data:job("GENERATING"),dataUpdateCount:7}})).toBe(2000)});it("terminal 상태에서 중단한다",()=>{expect(pollingInterval({state:{data:job("COMPLETED"),dataUpdateCount:2}})).toBe(false);expect(pollingInterval({state:{data:job("FAILED"),dataUpdateCount:2}})).toBe(false)});it("숨은 문서는 5초다",()=>{vi.spyOn(document,"hidden","get").mockReturnValue(true);expect(pollingInterval({state:{data:job("MIXING"),dataUpdateCount:2}})).toBe(5000)})});
