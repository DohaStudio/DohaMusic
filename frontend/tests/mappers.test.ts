import { describe,expect,it } from "vitest";import { mapPipelineStatus,mapSafeFiles } from "@/lib/mappers";
describe("DTO mappers",()=>{it("Pipeline 상태를 한국어 view로 매핑한다",()=>{expect(mapPipelineStatus("VOICE_CONVERTING")).toMatchObject({label:"음색 변환",tone:"active"});expect(mapPipelineStatus("FAILED").tone).toBe("error")});it("내부 file_path를 view에서 제거한다",()=>{const result=mapSafeFiles([{id:"f",job_id:"j",file_type:"final",file_path:"C:/private/song.wav",mime_type:"audio/wav",created_at:"2026-07-31T00:00:00Z"}]);expect(result[0]).not.toHaveProperty("filePath");expect(JSON.stringify(result)).not.toContain("private")})});
