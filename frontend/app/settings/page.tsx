"use client";
import { useState } from "react";
import { AppShell } from "@/components/app-shell";
import { ApiStatus } from "@/components/api-status";
export default function SettingsPage(){const [reduced,setReduced]=useState(false);return <AppShell><section className="page-stack narrow"><header className="page-heading"><p className="eyebrow">SETTINGS</p><h1>Studio 환경</h1><p>개인 브라우저 표시 설정과 Backend 연결 상태를 확인합니다.</p></header><div className="surface-card settings-list"><div><span>API Base URL</span><code>{process.env.NEXT_PUBLIC_API_BASE_URL??"/backend"}</code></div><div><span>Backend 상태</span><ApiStatus/></div><label><span>Theme</span><select aria-label="테마" defaultValue="dark" disabled><option value="dark">Premium Dark</option></select></label><label><span>움직임 줄이기</span><input type="checkbox" checked={reduced} onChange={(e)=>{setReduced(e.target.checked);document.documentElement.dataset.reduceMotion=String(e.target.checked)}}/></label><div><span>Polling</span><strong>초기 1초 · Foreground 2초 · Background 5초</strong></div><div><span>Frontend 상태</span><strong>Phase 8 MVP · Responsive Web</strong></div></div></section></AppShell>}
