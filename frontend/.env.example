# Browser requests use the same-origin Next.js proxy by default.
NEXT_PUBLIC_API_BASE_URL=/backend
# Server-only FastAPI origin used by next.config.ts rewrites.
DOHAMUSIC_API_ORIGIN=http://127.0.0.1:8000
